services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ezraamos/portfolio:latest
    container_name: nextjs-app-container
    ports:
      - '3000:7777'
    env_file:
      - .env
    restart: unless-stopped
